services:
  db:
    image: postgres:16
    container_name: gestao-jogos-db
    environment:
      POSTGRES_DB: gestao_jogos
      POSTGRES_USER: dev
      POSTGRES_PASSWORD: dev
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
