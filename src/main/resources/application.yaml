spring:
  application:
    name: gestao-jogos
  datasource:
    url: jdbc:postgresql://localhost:5432/gestao_jogos
    username: dev
    password: dev
  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false
  flyway:
    enabled: true
